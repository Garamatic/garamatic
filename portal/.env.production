# API base — empty for same-origin (nginx proxies to gatekeeper)
# In dev: http://localhost:8086
VITE_API_BASE=
VITE_GATEKEEPER_API_KEY=demo-api-key
# Set at runtime via env var or .env.local (NEVER commit real secrets)
VITE_PORTAL_SECRET=
