# Production environment variables
# VITE_API_BASE is set at build time or via nginx proxy
# VITE_GATEKEEPER_API_KEY and VITE_PORTAL_SECRET are injected at runtime
# NEVER commit real secrets to version control
